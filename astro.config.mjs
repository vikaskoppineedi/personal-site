// @ts-check
import { defineConfig } from 'astro/config';

// Served at the apex of the custom domain, so no `base` path is needed.
export default defineConfig({
  site: 'https://vikas.koppineedi.com',
});
